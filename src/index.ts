/**
 * Legacy AlphaRouter entrypoint — disabled after ethers v6 migration.
 * Use `npm run dev` / src/main.ts (SniperSearcher + flash loan path) instead.
 */
console.error(
  'src/index.ts (Uniswap AlphaRouter) is not supported on ethers v6. Run: bun run dev'
);
process.exit(1);
